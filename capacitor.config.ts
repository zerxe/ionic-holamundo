import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.holamundo',
  appName: 'ionic-holamundo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
